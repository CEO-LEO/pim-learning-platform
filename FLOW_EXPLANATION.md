# 📋 Flow การทำงานของระบบ Pre-test และ Post-test

## 🔄 Flow หลัก

```
┌─────────────────────────────────────────────────────────────┐
│  1. เข้าหน้า Module (/module/:moduleId)                      │
└─────────────────────────────────────────────────────────────┘
                        │
                        ▼
        ┌───────────────────────────────┐
        │  ดึงข้อมูล Quizzes ทั้งหมด      │
        │  - Pre-test (order_index = 0)  │
        │  - Post-test (order_index > 0) │
        └───────────────────────────────┘
                        │
        ┌───────────────┴───────────────┐
        │                               │
        ▼                               ▼
┌───────────────┐              ┌───────────────┐
│ Pre-test      │              │ Post-test      │
│ ยังไม่ทำ      │              │ (ทำได้เมื่อ    │
│               │              │  ดูวิดีโอครบ)   │
└───────────────┘              └───────────────┘
        │                               │
        ▼                               ▼
┌───────────────────────────────────────────────┐
│ 2. ทำ Pre-test (/quiz/:quizId)                │
│    - ไม่ต้องดูวิดีโอก่อน ✅                    │
│    - ทำได้ครั้งเดียว (allow_retake = 0)       │
│    - ตรวจสอบ: quiz.order_index === 0          │
└───────────────────────────────────────────────┘
                        │
                        ▼
        ┌───────────────────────────────┐
        │  ทำ Pre-test เสร็จ              │
        │  pretestDone = true            │
        └───────────────────────────────┘
                        │
                        ▼
┌───────────────────────────────────────────────┐
│ 3. ดูวิดีโอ (/video/:videoId)                  │
│    - ตรวจสอบ: ทำ Pre-test แล้วหรือยัง?         │
│    - ถ้ายังไม่ทำ → Error 403                   │
│    - ถ้าทำแล้ว → อนุญาตให้ดู ✅                │
└───────────────────────────────────────────────┘
                        │
                        ▼
        ┌───────────────────────────────┐
        │  ดูวิดีโอครบ 100%              │
        │  video.is_complete = 1         │
        └───────────────────────────────┘
                        │
                        ▼
┌───────────────────────────────────────────────┐
│ 4. ทำ Post-test (/quiz/:quizId)                │
│    - ตรวจสอบ: ดูวิดีโอครบ 100% หรือยัง?        │
│    - ถ้ายังไม่ครบ → Error 403                  │
│    - ถ้าครบแล้ว → อนุญาตให้ทำ ✅                │
│    - ตรวจสอบ: quiz.order_index > 0              │
└───────────────────────────────────────────────┘
```

## 📝 รายละเอียดแต่ละขั้นตอน

### 1. หน้า Module (`client/src/pages/Modules.js`)

**การทำงาน:**
```javascript
// ดึงข้อมูล quizzes
const response = await axios.get(`${API_URL}/quizzes/module/${moduleId}/all`);

// แยก pre-test และ post-test
const pretest = quizzes.find(q => q.order_index === 0);
const otherQuizzes = quizzes.filter(q => q.order_index !== 0);

// ตรวจสอบสถานะ pre-test
const pretestDone = pretest && pretest.last_passed !== -1;

// ถ้ายังไม่ทำ pre-test → disable วิดีโอ
<div className={!pretestDone ? 'opacity-50 pointer-events-none' : ''}>
```

### 2. Pre-test (`server/routes/quizzes.js` - GET /:quizId)

**การทำงาน:**
```javascript
// ตรวจสอบว่าเป็น pre-test หรือไม่
const isPretest = quiz.order_index === 0;

if (isPretest) {
  // Pre-test: ไม่ต้องตรวจสอบวิดีโอ
  // ดึงคำถามโดยตรง
  db.all('SELECT ... FROM quiz_questions WHERE quiz_id = ?', ...);
} else {
  // Post-test: ต้องตรวจสอบวิดีโอก่อน
  // ตรวจสอบ video.is_complete === 1
}
```

### 3. Video Access (`server/routes/videos.js` - GET /:videoId)

**การทำงาน:**
```javascript
// ตรวจสอบว่าได้ทำ pre-test แล้วหรือยัง
db.get(`
  SELECT v.*, 
    (SELECT COUNT(*) FROM quiz_results qr 
     JOIN quizzes q ON qr.quiz_id = q.quiz_id 
     WHERE q.module_id = v.module_id 
     AND q.order_index = 0 
     AND qr.user_id = ?) as pretest_done
  FROM videos v WHERE v.video_id = ?
`, [userId, videoId], (err, video) => {
  if (video.pretest_done === 0) {
    // ยังไม่ทำ pre-test → Error 403
    return res.status(403).json({ 
      error: 'กรุณาทำแบบทดสอบก่อนเรียน (Pre-test) ให้เสร็จก่อนเข้าชมวิดีโอ' 
    });
  }
  // ทำ pre-test แล้ว → อนุญาตให้ดู
});
```

### 4. Post-test (`server/routes/quizzes.js` - GET /:quizId)

**การทำงาน:**
```javascript
if (!isPretest) {
  // Post-test: ต้องตรวจสอบวิดีโอก่อน
  const requiredVideoOrder = quiz.order_index || 1;
  
  db.get(`
    SELECT v.video_id, v.order_index, 
           COALESCE(vp.is_complete, 0) as is_complete
    FROM videos v
    LEFT JOIN video_progress vp ON v.video_id = vp.video_id AND vp.user_id = ?
    WHERE v.module_id = ? AND v.order_index = ?
  `, [userId, quiz.module_id, requiredVideoOrder], (err, video) => {
    if (!video || video.is_complete !== 1) {
      // ยังไม่ดูวิดีโอครบ → Error 403
      return res.status(403).json({ 
        error: 'กรุณาดูวิดีโอให้ครบ 100% ก่อนทำแบบทดสอบ' 
      });
    }
    // ดูวิดีโอครบแล้ว → อนุญาตให้ทำ
  });
}
```

## 🔍 จุดตรวจสอบสำคัญ

### Pre-test (order_index = 0)
- ✅ **ไม่ต้องดูวิดีโอก่อน** - ข้ามการตรวจสอบวิดีโอ
- ✅ **ทำได้ครั้งเดียว** - `allow_retake = 0`
- ✅ **ต้องทำก่อนดูวิดีโอ** - ตรวจสอบที่ video endpoint

### Post-test (order_index > 0)
- ✅ **ต้องดูวิดีโอก่อน** - ตรวจสอบ `video.is_complete === 1`
- ✅ **ทำได้หลายครั้ง** - `allow_retake = 1` (ถ้าตั้งค่าไว้)
- ✅ **ตรวจสอบตาม order_index** - ต้องดูวิดีโอที่ `order_index` ตรงกัน

## 🐛 ปัญหาที่อาจพบ

1. **Error: "กรุณาดูวิดีโอให้ครบ 100% ก่อนทำแบบทดสอบ"**
   - เกิดเมื่อพยายามทำ post-test แต่ยังไม่ดูวิดีโอครบ
   - **แก้ไข:** ดูวิดีโอให้ครบ 100% ก่อน

2. **Error: "กรุณาทำแบบทดสอบก่อนเรียน (Pre-test) ให้เสร็จก่อนเข้าชมวิดีโอ"**
   - เกิดเมื่อพยายามดูวิดีโอแต่ยังไม่ทำ pre-test
   - **แก้ไข:** ทำ pre-test ให้เสร็จก่อน

3. **Pre-test ไม่แสดง**
   - ตรวจสอบว่าในฐานข้อมูลมี quiz ที่ `order_index = 0` หรือไม่
   - **แก้ไข:** รัน seed หรือสร้าง pre-test ผ่าน admin panel

## 📊 Database Schema

```sql
-- Quizzes table
CREATE TABLE quizzes (
  quiz_id TEXT PRIMARY KEY,
  module_id TEXT,
  title TEXT NOT NULL,
  order_index INTEGER DEFAULT 1,  -- 0 = Pre-test, >0 = Post-test
  allow_retake INTEGER DEFAULT 1,   -- 0 = ทำได้ครั้งเดียว, 1 = ทำได้หลายครั้ง
  ...
);

-- Quiz results
CREATE TABLE quiz_results (
  result_id TEXT PRIMARY KEY,
  user_id TEXT,
  quiz_id TEXT,
  score INTEGER,
  passed INTEGER,  -- 0 = ไม่ผ่าน, 1 = ผ่าน
  ...
);
```

## 🎯 สรุป

1. **Pre-test** → ทำก่อน (ไม่ต้องดูวิดีโอ) → ทำได้ครั้งเดียว
2. **Video** → ต้องทำ pre-test ก่อน → ดูให้ครบ 100%
3. **Post-test** → ต้องดูวิดีโอครบก่อน → ทำได้หลายครั้ง

