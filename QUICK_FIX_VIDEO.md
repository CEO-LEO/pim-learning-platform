# ⚡ แก้ไขปัญหา Video LFS Pointer อย่างรวดเร็ว

## 🎯 วิธีที่เร็วที่สุด: ใช้ Railway Dashboard

### ขั้นตอน (5 นาที):

1. **สร้าง Volume:**
   - ไปที่ https://railway.app → Project → Volumes
   - คลิก "New Volume"
   - Name: `video-storage`
   - Size: `10 GB`
   - Mount Path: `/app/server/uploads/videos`
   - คลิก "Create"

2. **อัปโหลดไฟล์:**
   - คลิกที่ Volume ที่สร้างไว้
   - คลิก "Upload Files" หรือ "Browse"
   - เลือกไฟล์ทั้งหมดจาก `server/uploads/videos/` ในเครื่อง
   - รอให้อัปโหลดเสร็จ (อาจใช้เวลาสักครู่)

3. **Restart Service:**
   - ไปที่ Railway Dashboard → Deployments
   - คลิก "Redeploy" หรือ restart service

4. **ทดสอบ:**
   - ไปที่แอปพลิเคชัน
   - ลองเปิดวิดีโอ
   - ควรจะเล่นได้แล้ว! ✅

---

## 🔍 ตรวจสอบว่าแก้ไขแล้ว

```bash
# SSH เข้า Railway
railway shell

# ตรวจสอบไฟล์
ls -lh /app/server/uploads/videos/
file /app/server/uploads/videos/video-module_1-1.mp4
```

**ควรเห็น:**
- ไฟล์มีขนาดใหญ่ (ไม่ใช่ 100-200 bytes)
- `file` command บอกว่าเป็น video file (ไม่ใช่ text file)

---

## 🚨 ถ้ายังมีปัญหา

### ปัญหา: ไฟล์ยังเป็น LFS pointer

**แก้ไข:**
1. ตรวจสอบว่า Volume mount path ถูกต้อง
2. ตรวจสอบว่าไฟล์อัปโหลดเสร็จแล้ว
3. Restart service

### ปัญหา: วิดีโอไม่เล่น

**ตรวจสอบ:**
1. เปิด Browser DevTools (F12)
2. Console tab → หา error messages
3. Network tab → ตรวจสอบ request `/api/video-stream/...`
4. Status code ควรเป็น 200 หรือ 206

---

## 📋 Checklist

- [ ] สร้าง Railway Volume
- [ ] Mount path: `/app/server/uploads/videos`
- [ ] อัปโหลดไฟล์วิดีโอทั้งหมด
- [ ] Restart service
- [ ] ตรวจสอบไฟล์บน server
- [ ] ทดสอบวิดีโอ

---

## 💡 วิธีอื่น (ถ้าวิธีแรกไม่ทำงาน)

ดู `SETUP_VIDEO_STORAGE.md` สำหรับวิธีอื่น:
- ใช้ Railway CLI
- ใช้ External Storage (Cloudinary, S3, etc.)

