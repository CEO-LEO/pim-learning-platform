# 🎓 PIM Learning Platform

แพลตฟอร์มการเรียนรู้สำหรับมหาวิทยาลัยปัญญาภิวัฒน์ (PIM University)

## ✨ ฟีเจอร์หลัก

- 🔐 ระบบยืนยันตัวตน (Login, Register, SSO)
- 🎥 เรียนรู้ผ่านวิดีโอ (5 หมวด)
- 📝 ทำแบบทดสอบ (Quiz System)
- 🧪 ลงทะเบียนสอบปฏิบัติ (Practical Exam Registration)
- 📊 สรุปผลการทดสอบ (Assessment Results)
- ⏱ สะสมชั่วโมงฝึก (Learning Hours Tracking)
- 👨‍🎓 รองรับนักศึกษาทุกชั้นปี (Level System)
- 📝 ประเมินหลังฝึกงาน (Post-Training Evaluation)
- 📊 สรุปสถิติรายบุคคล (Student Analytics)

## 🚀 การติดตั้ง

### 1. ติดตั้ง dependencies

```bash
npm run install-all
```

### 2. ตั้งค่า Environment Variables

**สำคัญ:** ต้องตั้งค่า JWT_SECRET ก่อนรันเซิร์ฟเวอร์

สร้างไฟล์ `server/.env` จาก `server/env.example.txt`:

```bash
cd server
# Windows
copy env.example.txt .env
# Linux/Mac
cp env.example.txt .env
```

แก้ไขค่าใน `.env`:
- **JWT_SECRET** (จำเป็น): สร้าง secret key ที่แข็งแรงสำหรับ production
  ```bash
  # ตัวอย่างการสร้าง secret key (Linux/Mac)
  openssl rand -base64 32
  ```

**หมายเหตุ:** หากไม่ตั้งค่า JWT_SECRET เซิร์ฟเวอร์จะไม่สามารถรันได้ (เพื่อความปลอดภัย)

### 3. รันแอปพลิเคชัน

```bash
npm run dev
```

## 👤 บัญชีเริ่มต้น

- **Admin**: `ADMIN001` / `admin123`
- **Instructor**: `INST001` / `instructor123`
- **Student**: `STU001` / `student123`

## 📋 การจัดการรายชื่อนักเรียน (Whitelist)

ระบบจะอนุญาตให้เฉพาะนักเรียนที่มีชื่อใน whitelist เท่านั้นที่สามารถเข้าสู่ระบบได้

### นำเข้าข้อมูลนักเรียนจาก CSV

1. สร้างไฟล์ CSV ด้วยรูปแบบต่อไปนี้:
   ```csv
   student_id,name,phone,gpax,advisor
   8840000028,นางสาวศิลารัตน์ เชิงหอม,0820862960,3.18,ผู้ช่วยศาสตราจารย์สิทธิพัทธ์ เลิศศรีชัยนนท์
   ```

2. รันคำสั่ง import:
   ```bash
   cd server/database
   node bulk-import-students.js students.csv
   ```

3. รหัสผ่านเริ่มต้นสำหรับนักเรียนทุกคนคือ **รหัสนักศึกษา** (student_id)

### ตัวอย่างไฟล์ CSV

ดูไฟล์ `server/database/students-template.csv` สำหรับรูปแบบที่ถูกต้อง

**หมายเหตุ:** 
- นักเรียนที่ไม่อยู่ใน whitelist จะไม่สามารถเข้าสู่ระบบได้
- Admin และ Instructor สามารถเข้าสู่ระบบได้เสมอ

## 🚀 การ Deploy ขึ้นเซิร์ฟเวอร์

### ⚡ Deploy บน Vercel (แนะนำ - ง่ายที่สุด)

**Frontend → Vercel | Backend → Railway/Render**

ดูคู่มือแบบเร็ว: `VERCEL_QUICK_START.md`  
ดูคู่มือแบบละเอียด: `VERCEL_DEPLOY.md`

**Quick Start:**
```bash
# ใช้ script
./deploy-vercel.sh  # Linux/Mac
# หรือ
deploy-vercel.bat   # Windows
```

### 🐳 Deploy ด้วย Docker

ดูคู่มือ: `DEPLOYMENT.md`

```bash
docker-compose up -d
```

### 🖥️ Deploy บน VPS

ดูคู่มือ: `DEPLOYMENT.md`

## 🎨 ธีมสี

- น้ำเงิน (Blue): `#2563eb`
- ฟ้า (Light Blue): `#3b82f6`, `#0ea5e9`
- ขาว (White): `#ffffff`

