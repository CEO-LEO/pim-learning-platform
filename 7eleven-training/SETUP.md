# 🚀 คู่มือการ Setup ระบบ 7-Eleven Training

## 📋 สารบัญ

1. [ข้อกำหนดเบื้องต้น](#ข้อกำหนดเบื้องต้น)
2. [การตั้งค่า Supabase](#การตั้งค่า-supabase)
3. [การตั้งค่า Environment Variables](#การตั้งค่า-environment-variables)
4. [การรัน Prisma Migrations](#การรัน-prisma-migrations)
5. [การ Import ข้อมูลนักเรียน](#การ-import-ข้อมูลนักเรียน)
6. [การตรวจสอบสถานะระบบ](#การตรวจสอบสถานะระบบ)
7. [การแก้ไขปัญหา](#การแก้ไขปัญหา)

---

## ข้อกำหนดเบื้องต้น

- ✅ Node.js (v18 หรือสูงกว่า)
- ✅ npm หรือ yarn
- ✅ Supabase Account (ฟรี)
- ✅ SQLite Database จาก PIMX project (`C:\PIMX\server\database\pim_learning.db`)

---

## การตั้งค่า Supabase

### 1. สร้าง Supabase Project

1. ไปที่ [https://supabase.com](https://supabase.com)
2. สร้าง Account (ถ้ายังไม่มี)
3. คลิก "New Project"
4. ตั้งชื่อ Project และเลือก Region (แนะนำ: Southeast Asia)
5. ตั้งรหัสผ่าน Database (บันทึกไว้!)
6. รอให้ Project สร้างเสร็จ (ประมาณ 2-3 นาที)

### 2. รับ Supabase Credentials

1. ไปที่ Project Settings → API
2. คัดลอก:
   - **Project URL** (เช่น: `https://xxxxx.supabase.co`)
   - **anon/public key** (เริ่มต้นด้วย `eyJ...`)

### 3. รับ Database Connection String

1. ไปที่ Project Settings → Database
2. คลิก "Connection string" → "URI"
3. คัดลอก connection string (เช่น: `postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres`)
4. แทนที่ `[PASSWORD]` ด้วยรหัสผ่าน Database ที่ตั้งไว้ตอนสร้าง Project

---

## การตั้งค่า Environment Variables

### 1. สร้างไฟล์ `.env.local`

สร้างไฟล์ `.env.local` ในโฟลเดอร์ `7eleven-training`:

```env
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Database Connection (สำหรับ Prisma)
DATABASE_URL=postgresql://postgres:your-password@db.your-project-ref.supabase.co:5432/postgres
```

### 2. แทนที่ค่าด้วยข้อมูลจริง

- แทนที่ `your-project-id` ด้วย Project ID จริง
- แทนที่ `your-anon-key-here` ด้วย anon key จริง
- แทนที่ `your-password` ด้วยรหัสผ่าน Database
- แทนที่ `your-project-ref` ด้วย Project Reference

---

## การรัน Prisma Migrations

### วิธีที่ 1: ใช้สคริปต์ Setup (แนะนำ)

```bash
npm run setup
```

สคริปต์จะ:
- ✅ ตรวจสอบ Supabase credentials
- ✅ ตรวจสอบการเชื่อมต่อ
- ✅ รัน Prisma migrations อัตโนมัติ (ถ้ายังไม่ได้รัน)
- ✅ Import ข้อมูลนักเรียน

### วิธีที่ 2: รันด้วยตนเอง

```bash
# Generate Prisma Client
npx prisma generate

# Run migrations
npx prisma migrate dev --name init
```

---

## การ Import ข้อมูลนักเรียน

### วิธีที่ 1: ใช้สคริปต์ Setup (แนะนำ)

```bash
npm run setup
```

### วิธีที่ 2: Import แยก

```bash
npm run import-students
```

### สิ่งที่สคริปต์จะทำ:

1. ✅ อ่านข้อมูลนักเรียนจาก SQLite database (`C:\PIMX\server\database\pim_learning.db`)
2. ✅ ตรวจสอบว่านักเรียนมีอยู่ใน Supabase แล้วหรือไม่
3. ✅ Import ข้อมูลเข้า Supabase (ข้ามถ้ามีอยู่แล้ว)
4. ✅ สร้าง password hash สำหรับนักเรียนที่ยังไม่มี (default: `student123`)
5. ✅ แสดงสรุปผลการ import

### ข้อมูลที่จะ Import:

- `student_id` - รหัสนักศึกษา
- `name` - ชื่อ-นามสกุล
- `email` - อีเมล (หรือสร้างจาก `student_id@pim.ac.th`)
- `password_hash` - รหัสผ่านที่ hash แล้ว
- `phone` - เบอร์โทรศัพท์
- `role` - บทบาท (student)

---

## การตรวจสอบสถานะระบบ

### รันสคริปต์ตรวจสอบ

```bash
npm run check-status
```

สคริปต์จะตรวจสอบ:

1. ✅ Environment Variables (Supabase URL, Key, Database URL)
2. ✅ Prisma Migrations
3. ✅ SQLite Database (จำนวนนักเรียน)
4. ✅ Supabase Connection (จำนวนนักเรียนใน Supabase)

---

## การแก้ไขปัญหา

### ❌ Error: "Supabase credentials ยังเป็น placeholder"

**สาเหตุ:** ยังไม่ได้ตั้งค่า Supabase credentials ใน `.env.local`

**วิธีแก้:**
1. เปิดไฟล์ `.env.local`
2. แทนที่ `your-project-id` และ `your-anon-key-here` ด้วยข้อมูลจริง
3. รัน `npm run check-status` เพื่อตรวจสอบ

---

### ❌ Error: "ตาราง 'users' ไม่มีใน Supabase database"

**สาเหตุ:** ยังไม่ได้รัน Prisma migrations

**วิธีแก้:**
```bash
npx prisma migrate dev
```

---

### ❌ Error: "DATABASE_URL ไม่ได้ตั้งค่า"

**สาเหตุ:** ยังไม่ได้ตั้งค่า `DATABASE_URL` ใน `.env.local`

**วิธีแก้:**
1. ไปที่ Supabase Dashboard → Settings → Database
2. คัดลอก Connection String (URI)
3. แทนที่ `[PASSWORD]` ด้วยรหัสผ่าน Database
4. เพิ่มใน `.env.local`:
   ```
   DATABASE_URL=postgresql://postgres:your-password@db.xxxxx.supabase.co:5432/postgres
   ```

---

### ❌ Error: "ไม่สามารถเชื่อมต่อกับ Supabase ได้"

**สาเหตุ:**
- Internet connection มีปัญหา
- Supabase URL หรือ Key ไม่ถูกต้อง
- Supabase project ถูก pause หรือลบ

**วิธีแก้:**
1. ตรวจสอบ Internet connection
2. ตรวจสอบ Supabase URL และ Key ใน `.env.local`
3. ตรวจสอบ Supabase Dashboard ว่า Project ยัง active อยู่

---

### ❌ Error: "ไม่พบ SQLite database"

**สาเหตุ:** ไฟล์ `C:\PIMX\server\database\pim_learning.db` ไม่มี

**วิธีแก้:**
1. ตรวจสอบว่า PIMX project มี SQLite database
2. ตรวจสอบ path ที่ถูกต้อง
3. Import ข้อมูลนักเรียนเข้า SQLite ก่อน (ใช้สคริปต์ใน PIMX project)

---

### ⚠️ จำนวนนักเรียนใน Supabase น้อยกว่าใน SQLite

**สาเหตุ:** บางคนอาจมีอยู่แล้วใน Supabase (ถูกข้าม)

**วิธีแก้:**
- นี่เป็นพฤติกรรมปกติ (สคริปต์จะข้ามนักเรียนที่มีอยู่แล้ว)
- ตรวจสอบด้วย `npm run check-status`

---

## 📝 สรุปคำสั่งที่ใช้บ่อย

```bash
# ตรวจสอบสถานะระบบ
npm run check-status

# Setup ทั้งหมด (migrations + import)
npm run setup

# Import ข้อมูลนักเรียน (ถ้า migrations รันแล้ว)
npm run import-students

# รัน Prisma migrations
npx prisma migrate dev

# Generate Prisma Client
npx prisma generate

# เปิด Prisma Studio (ดูข้อมูลใน database)
npx prisma studio
```

---

## 🎉 เสร็จสมบูรณ์!

เมื่อ setup เสร็จแล้ว คุณจะสามารถ:

- ✅ เข้าใช้งานระบบ 7-Eleven Training
- ✅ Login ด้วยรหัสนักศึกษาและรหัสผ่าน
- ✅ ดูข้อมูลนักเรียนทั้งหมดใน Supabase

**รหัสผ่านเริ่มต้น:** `student123` (สำหรับนักเรียนที่ import จาก SQLite)

---

## 📞 ติดต่อ

หากมีปัญหาหรือคำถาม กรุณาตรวจสอบ:
1. Error messages ใน terminal
2. Supabase Dashboard → Logs
3. Prisma documentation: https://www.prisma.io/docs

