# 🚀 คู่มือการ Setup ระบบ 7-Eleven Training

## ⚡ Quick Start (3 ขั้นตอน)

### ขั้นตอนที่ 1: ตั้งค่า Supabase Credentials

**วิธีที่ 1: ใช้สคริปต์ (แนะนำ)**
```bash
npm run quick-setup
```

**วิธีที่ 2: Setup ครบถ้วน**
```bash
npm run setup-complete
```

**วิธีที่ 3: แก้ไขด้วยตนเอง**
1. เปิดไฟล์ `.env.local`
2. แก้ไข:
   ```env
   NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
   DATABASE_URL=postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres
   ```

**หา Supabase Credentials:**
1. ไปที่ https://supabase.com
2. สร้าง Account และ New Project
3. ไปที่ **Settings** → **API**
4. คัดลอก **Project URL** และ **anon public** key
5. สำหรับ DATABASE_URL: ไปที่ **Settings** → **Database** → **Connection string** → **URI**

---

### ขั้นตอนที่ 2: รัน Prisma Migrations

```bash
npx prisma migrate dev
```

คำสั่งนี้จะสร้างตารางทั้งหมดใน Supabase database

---

### ขั้นตอนที่ 3: สร้าง Admin User

```bash
npm run create-admin ADMIN001 admin1234
```

---

## ✅ ตรวจสอบสถานะ

```bash
npm run check-status
```

หรือ

```bash
npm run guide
```

---

## 🔑 รหัสเข้าสู่ระบบ

**Admin:**
- รหัสนักศึกษา: `ADMIN001`
- รหัสผ่าน: `admin1234`

**นักเรียน:**
- รหัสผ่านเริ่มต้น: `student123`
- รหัสนักศึกษา: ใช้รหัสจากฐานข้อมูล

---

## 🎯 เริ่มใช้งาน

หลังจาก setup เสร็จแล้ว:
1. ✅ Login ที่ http://localhost:3000/login
2. ✅ ใช้รหัส Admin: `ADMIN001` / `admin1234`
3. ✅ เริ่มใช้งานระบบ!

---

## ⚠️ แก้ไขปัญหา

### ❌ Error: "ตาราง users ไม่มีใน Supabase"
**แก้ไข:**
```bash
npx prisma migrate dev
```

### ❌ Error: "Supabase credentials ไม่ถูกต้อง"
**แก้ไข:**
1. ตรวจสอบไฟล์ `.env.local`
2. ตรวจสอบว่า Supabase project ยัง active อยู่
3. รัน `npm run quick-setup` อีกครั้ง

### ❌ Error: "DATABASE_URL ไม่ได้ตั้งค่า"
**แก้ไข:**
เพิ่ม DATABASE_URL ใน `.env.local`:
```env
DATABASE_URL=postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres
```

---

## 📚 เอกสารเพิ่มเติม

- **[QUICK_START.md](./QUICK_START.md)** - คู่มือเริ่มต้นแบบย่อ
- **[SETUP_GUIDE.md](./SETUP_GUIDE.md)** - คู่มือ Setup แบบละเอียด
- **[README.md](./README.md)** - เอกสารหลักของโปรเจกต์

