# 📋 คำสั่งทั้งหมดในระบบ 7-Eleven Training

## 🚀 คำสั่งหลัก

### Setup และ Configuration

| คำสั่ง | คำอธิบาย | เมื่อไหร่ใช้ |
|--------|----------|-------------|
| `npm run init-env` | ตั้งค่า environment variables แบบ interactive | ครั้งแรกที่ setup |
| `npm run verify` | ตรวจสอบความพร้อมของระบบ | หลังตั้งค่า credentials |
| `npm run check-status` | ตรวจสอบสถานะระบบ (ละเอียด) | ตรวจสอบสถานะทั่วไป |
| `npm run test-connection` | ทดสอบการเชื่อมต่อ Supabase | ตรวจสอบการเชื่อมต่อ |
| `npm run setup` | Setup ทั้งหมด (migrations + import) | Setup ครั้งแรก |

### Data Management

| คำสั่ง | คำอธิบาย | เมื่อไหร่ใช้ |
|--------|----------|-------------|
| `npm run import-students` | Import ข้อมูลนักเรียนจาก SQLite | หลัง migrations |
| `npm run validate-data` | ตรวจสอบความถูกต้องของข้อมูล SQLite | ก่อน import |
| `npm run report` | สร้างรายงานสรุประบบ | ตรวจสอบสถานะ |

### Development

| คำสั่ง | คำอธิบาย | เมื่อไหร่ใช้ |
|--------|----------|-------------|
| `npm run dev` | เริ่ม development server | พัฒนาแอป |
| `npm run build` | Build สำหรับ production | ก่อน deploy |
| `npm run start` | เริ่ม production server | หลัง build |
| `npm run lint` | ตรวจสอบ code quality | ตรวจสอบโค้ด |

### Database (Prisma)

| คำสั่ง | คำอธิบาย | เมื่อไหร่ใช้ |
|--------|----------|-------------|
| `npx prisma migrate dev` | สร้างและรัน migrations | เปลี่ยน schema |
| `npx prisma generate` | Generate Prisma Client | หลังเปลี่ยน schema |
| `npx prisma studio` | เปิด Prisma Studio (ดูข้อมูล) | ดู/แก้ไขข้อมูล |
| `npx prisma validate` | ตรวจสอบ Prisma schema | ตรวจสอบ schema |
| `npm run migration-info` | ข้อมูลเกี่ยวกับ migrations | ตรวจสอบ migrations |

---

## 📝 Workflow แนะนำ

### 1. Setup ครั้งแรก

```bash
# 1. ติดตั้ง dependencies
npm install

# 2. ตั้งค่า environment variables
npm run init-env

# 3. ตรวจสอบความพร้อม
npm run verify

# 4. Setup ทั้งหมด
npm run setup
```

### 2. ตรวจสอบข้อมูลก่อน Import

```bash
# 1. ตรวจสอบข้อมูล SQLite
npm run validate-data

# 2. ตรวจสอบสถานะระบบ
npm run check-status

# 3. Import ข้อมูล
npm run import-students
```

### 3. ตรวจสอบสถานะระบบ

```bash
# วิธีที่ 1: ตรวจสอบแบบย่อ
npm run verify

# วิธีที่ 2: ตรวจสอบแบบละเอียด
npm run check-status

# วิธีที่ 3: สร้างรายงาน
npm run report
```

### 4. Development

```bash
# 1. เริ่ม development server
npm run dev

# 2. ดูข้อมูลใน database
npx prisma studio
```

---

## 🔍 คำสั่งตรวจสอบ

### ตรวจสอบ Environment Variables
```bash
npm run check-status
```

### ตรวจสอบการเชื่อมต่อ Supabase
```bash
npm run test-connection
```

### ตรวจสอบข้อมูล SQLite
```bash
npm run validate-data
```

### ตรวจสอบ Prisma Schema
```bash
npx prisma validate
```

---

## 📊 คำสั่งรายงาน

### สร้างรายงานสรุป
```bash
npm run report
```
รายงานจะถูกบันทึกไว้ที่ `system-report.json`

---

## 🛠️ คำสั่งแก้ไขปัญหา

### Reset Database (⚠️ จะลบข้อมูลทั้งหมด)
```bash
npx prisma migrate reset
```

### สร้าง Migration ใหม่
```bash
npx prisma migrate dev --name migration_name
```

### Generate Prisma Client ใหม่
```bash
npx prisma generate
```

---

## 📚 เอกสารเพิ่มเติม

- **[QUICK_START.md](./QUICK_START.md)** - คู่มือเริ่มต้นใช้งาน
- **[SETUP.md](./SETUP.md)** - คู่มือการ Setup แบบละเอียด
- **[TROUBLESHOOTING.md](./TROUBLESHOOTING.md)** - คู่มือการแก้ไขปัญหา
- **[README-IMPORT.md](./README-IMPORT.md)** - คำแนะนำการ Import ข้อมูล

---

## 💡 Tips

1. **ใช้ `npm run verify` ก่อน setup** เพื่อตรวจสอบความพร้อม
2. **ใช้ `npm run validate-data` ก่อน import** เพื่อตรวจสอบข้อมูล
3. **ใช้ `npm run report` หลัง import** เพื่อตรวจสอบผลลัพธ์
4. **ใช้ `npx prisma studio`** เพื่อดูและแก้ไขข้อมูลใน database
5. **ใช้ `npm run check-status`** เมื่อมีปัญหาเพื่อดูสถานะปัจจุบัน

---

## 🎯 Quick Reference

```bash
# Setup
npm run init-env          # ตั้งค่า credentials
npm run setup             # Setup ทั้งหมด

# ตรวจสอบ
npm run verify            # ตรวจสอบความพร้อม
npm run check-status      # ตรวจสอบสถานะ
npm run validate-data     # ตรวจสอบข้อมูล
npm run test-connection   # ทดสอบการเชื่อมต่อ
npm run report            # สร้างรายงาน

# Import
npm run import-students  # Import ข้อมูล

# Development
npm run dev               # เริ่ม dev server
npx prisma studio         # เปิด Prisma Studio
```

