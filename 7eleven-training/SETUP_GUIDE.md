# 🚀 คู่มือการตั้งค่า Supabase สำหรับ 7-Eleven Training System

## ขั้นตอนที่ 1: สร้าง Supabase Project

### 1.1 สร้าง Account (ถ้ายังไม่มี)
1. ไปที่ [https://supabase.com](https://supabase.com)
2. คลิก "Start your project" หรือ "Sign Up"
3. สร้าง Account ด้วย GitHub, Google, หรือ Email

### 1.2 สร้าง New Project
1. หลังจาก Login แล้ว คลิก "New Project"
2. กรอกข้อมูล:
   - **Name**: ตั้งชื่อโปรเจกต์ (เช่น `7eleven-training`)
   - **Database Password**: ตั้งรหัสผ่านที่แข็งแรง (จำไว้! ต้องใช้ตอนสร้าง Database URL)
   - **Region**: เลือก region ที่ใกล้ที่สุด (เช่น `Southeast Asia (Singapore)`)
3. คลิก "Create new project"
4. รอให้สร้างเสร็จ (ประมาณ 2-3 นาที)

## ขั้นตอนที่ 2: หา Supabase Credentials

### 2.1 Project URL
1. ไปที่ Dashboard ของ Project
2. คลิก **Settings** (ไอคอนฟันเฟือง) ที่แถบซ้าย
3. คลิก **API** ในเมนู Settings
4. คัดลอก **Project URL** (รูปแบบ: `https://xxxxx.supabase.co`)

### 2.2 Anon Key
1. ในหน้า API Settings เดียวกัน
2. หา **Project API keys**
3. คัดลอก **anon public** key (รูปแบบ: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`)

### 2.3 Database URL (Optional - สำหรับ Prisma)
1. ใน Settings คลิก **Database**
2. หา **Connection string** → **URI**
3. คัดลอก URL และแทนที่ `[YOUR-PASSWORD]` ด้วยรหัสผ่านที่ตั้งไว้ตอนสร้าง project
   - รูปแบบ: `postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres`

## ขั้นตอนที่ 3: ตั้งค่าในโปรเจกต์

### วิธีที่ 1: ใช้หน้า Setup Guide (แนะนำ)
1. เปิดเบราว์เซอร์ไปที่: `http://localhost:3000/setup`
2. กรอกข้อมูลที่ได้จากขั้นตอนที่ 2
3. คลิก "💾 บันทึกการตั้งค่า"
4. รอให้บันทึกเสร็จ (จะรีเฟรชหน้าเว็บอัตโนมัติ)

### วิธีที่ 2: ใช้ Terminal Script
```bash
cd 7eleven-training
npm run quick-setup
```
แล้วกรอกข้อมูลตามที่ถาม

### วิธีที่ 3: แก้ไขไฟล์ .env.local ด้วยตนเอง
1. สร้างหรือแก้ไขไฟล์ `.env.local` ในโฟลเดอร์ `7eleven-training`
2. เพิ่มเนื้อหาดังนี้:
```env
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Database Connection (Optional)
DATABASE_URL=postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres
```
3. แทนที่ `xxxxx` และ `eyJ...` ด้วยค่าจริงที่ได้จาก Supabase

## ขั้นตอนที่ 4: ตรวจสอบการตั้งค่า

### ตรวจสอบว่า Supabase ตั้งค่าถูกต้อง
```bash
npm run check-status
```

### ตรวจสอบการเชื่อมต่อ
```bash
npm run test-connection
```

## ขั้นตอนที่ 5: Setup Database Schema

### 5.1 รัน Prisma Migrations (ถ้ามี Prisma)
```bash
npx prisma migrate dev
```

### 5.2 หรือใช้ SQL Scripts
1. ไปที่ Supabase Dashboard → SQL Editor
2. รัน SQL scripts จากโฟลเดอร์ `database/` หรือ `migrations/`

## ขั้นตอนที่ 6: สร้าง Admin User

```bash
npm run create-admin ADMIN001 admin1234
```

หรือใช้หน้า Setup Guide ที่มีปุ่มสร้าง Admin

## ขั้นตอนที่ 7: ทดสอบการ Login

1. เปิดเบราว์เซอร์ไปที่: `http://localhost:3000/login`
2. ใช้รหัส:
   - **Student ID**: `ADMIN001`
   - **Password**: `admin1234`
3. ควรจะ Login สำเร็จและไปที่ Admin Dashboard

## 🔧 Troubleshooting

### ปัญหา: Warning "Supabase ยังไม่ได้ตั้งค่า" ยังแสดงอยู่
**แก้ไข:**
1. ตรวจสอบว่าไฟล์ `.env.local` มีอยู่และมีข้อมูลถูกต้อง
2. Restart development server:
   ```bash
   # หยุด server (Ctrl+C)
   npm run dev
   ```
3. รีเฟรชหน้าเว็บ (Ctrl+F5)

### ปัญหา: "Invalid API key" หรือ "Connection refused"
**แก้ไข:**
1. ตรวจสอบว่า Project URL และ Anon Key ถูกต้อง
2. ตรวจสอบว่า Supabase Project ยัง Active อยู่ (ไม่ถูก Suspend)
3. ตรวจสอบ Network/Firewall settings

### ปัญหา: "User not found" ตอน Login
**แก้ไข:**
1. รันคำสั่งสร้าง Admin อีกครั้ง:
   ```bash
   npm run create-admin ADMIN001 admin1234
   ```
2. ตรวจสอบว่า Database มีตาราง `users` และมีข้อมูล

## 📚 ข้อมูลเพิ่มเติม

- [Supabase Documentation](https://supabase.com/docs)
- [Supabase Dashboard](https://app.supabase.com)
- [Next.js Environment Variables](https://nextjs.org/docs/basic-features/environment-variables)

## ✅ Checklist

- [ ] สร้าง Supabase Account
- [ ] สร้าง Supabase Project
- [ ] คัดลอก Project URL
- [ ] คัดลอก Anon Key
- [ ] ตั้งค่าใน .env.local
- [ ] Restart development server
- [ ] ตรวจสอบการตั้งค่า (npm run check-status)
- [ ] Setup Database Schema
- [ ] สร้าง Admin User
- [ ] ทดสอบ Login

---

**หมายเหตุ:** หลังจากตั้งค่า Supabase แล้ว Warning message จะหายไปอัตโนมัติ
