# 🚀 วิธีตั้งค่า Supabase สำหรับ 7-Eleven Training System

## 📋 ขั้นตอนการ Setup

### ขั้นตอนที่ 1: สร้าง Supabase Project (ถ้ายังไม่มี)

1. **ไปที่ https://supabase.com**
2. **สร้าง Account** (ถ้ายังไม่มี)
   - คลิก "Start your project"
   - Sign up ด้วย Email หรือ GitHub
3. **สร้าง New Project**
   - คลิก "New Project"
   - กรอกข้อมูล:
     - **Name**: ตั้งชื่อโปรเจกต์ (เช่น `7eleven-training`)
     - **Database Password**: ตั้งรหัสผ่านที่แข็งแรง (จำไว้!)
     - **Region**: เลือก region ที่ใกล้ที่สุด (เช่น `Southeast Asia (Singapore)`)
   - คลิก "Create new project"
   - รอให้ project สร้างเสร็จ (ประมาณ 2-3 นาที)

---

### ขั้นตอนที่ 2: หา Supabase Credentials

หลังจาก project สร้างเสร็จแล้ว:

1. **ไปที่ Settings → API**
2. **คัดลอก Project URL**
   - ดูที่ "Project URL"
   - คัดลอก URL (เช่น `https://xxxxx.supabase.co`)
3. **คัดลอก anon public key**
   - ดูที่ "Project API keys"
   - คัดลอก "anon public" key (เริ่มต้นด้วย `eyJ...`)

---

### ขั้นตอนที่ 3: หา Database URL

1. **ไปที่ Settings → Database**
2. **คลิก "Connection string"**
3. **เลือก "URI"**
4. **คัดลอก connection string**
   - จะได้รูปแบบ: `postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres`
   - **แทนที่ `[PASSWORD]` ด้วยรหัสผ่านที่ตั้งไว้ตอนสร้าง project**

---

### ขั้นตอนที่ 4: ตั้งค่าในโปรเจกต์

#### วิธีที่ 1: ใช้สคริปต์ Interactive (แนะนำ)

```bash
npm run quick-setup
```

สคริปต์จะถามข้อมูลทีละข้อ:
1. Supabase Project URL → วาง URL ที่คัดลอกมา
2. Supabase Anon Key → วาง key ที่คัดลอกมา
3. Database URL → วาง connection string ที่แก้ไขแล้ว

#### วิธีที่ 2: แก้ไขไฟล์ .env.local

1. **เปิดไฟล์ `.env.local`** (หรือคัดลอกจาก `.env.local.example`)
2. **แก้ไขข้อมูล:**
   ```env
   NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   DATABASE_URL=postgresql://postgres:your-password@db.xxxxx.supabase.co:5432/postgres
   ```
3. **บันทึกไฟล์**

---

### ขั้นตอนที่ 5: ตรวจสอบการตั้งค่า

```bash
npm run check-status
```

หรือ

```bash
npm run guide
```

ควรเห็น:
- ✅ Supabase URL: ตั้งค่าแล้ว
- ✅ Supabase Key: ตั้งค่าแล้ว
- ✅ Database URL: ตั้งค่าแล้ว

---

### ขั้นตอนที่ 6: รัน Prisma Migrations

```bash
npx prisma migrate dev
```

คำสั่งนี้จะสร้างตารางทั้งหมดใน Supabase database

---

### ขั้นตอนที่ 7: สร้าง Admin User

```bash
npm run create-admin ADMIN001 admin1234
```

---

### ขั้นตอนที่ 8: Login

1. ไปที่ http://localhost:3000/login
2. ใช้รหัส:
   - รหัสนักศึกษา: `ADMIN001`
   - รหัสผ่าน: `admin1234`

---

## ✅ เสร็จสมบูรณ์!

ตอนนี้ระบบพร้อมใช้งานแล้ว! 🎉

---

## ⚠️ แก้ไขปัญหา

### ❌ Error: "ตาราง users ไม่มีใน Supabase"
**แก้ไข:**
```bash
npx prisma migrate dev
```

### ❌ Error: "Supabase credentials ไม่ถูกต้อง"
**แก้ไข:**
1. ตรวจสอบว่า URL และ Key ถูกต้อง
2. ตรวจสอบว่า Supabase project ยัง active อยู่
3. รัน `npm run quick-setup` อีกครั้ง

### ❌ Error: "DATABASE_URL ไม่ได้ตั้งค่า"
**แก้ไข:**
เพิ่ม DATABASE_URL ใน `.env.local`:
```env
DATABASE_URL=postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres
```

---

## 📚 เอกสารเพิ่มเติม

- **[SETUP_STEPS.md](./SETUP_STEPS.md)** - คู่มือ Setup แบบย่อ
- **[SETUP_GUIDE.md](./SETUP_GUIDE.md)** - คู่มือ Setup แบบละเอียด
- **[QUICK_START.md](./QUICK_START.md)** - คู่มือเริ่มต้น

