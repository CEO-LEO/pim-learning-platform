# 📥 คำแนะนำการ Import ข้อมูลนักเรียนเข้า Supabase

> **หมายเหตุ:** สำหรับคำแนะนำการ Setup ระบบแบบครบถ้วน กรุณาดู [SETUP.md](./SETUP.md)

## 🚀 Quick Start

### วิธีที่ 1: Setup ทั้งหมด (แนะนำ)

```bash
npm run setup
```

สคริปต์จะทำทุกอย่างให้อัตโนมัติ:
- ✅ ตรวจสอบ Supabase credentials
- ✅ ตรวจสอบการเชื่อมต่อ
- ✅ รัน Prisma migrations (ถ้ายังไม่ได้รัน)
- ✅ Import ข้อมูลนักเรียน

### วิธีที่ 2: Import แยก

```bash
npm run import-students
```

---

## 📋 ข้อกำหนดเบื้องต้น

1. ✅ Supabase Project สร้างแล้ว
2. ✅ `.env.local` ตั้งค่าแล้ว (ดู [SETUP.md](./SETUP.md))
3. ✅ Prisma migrations รันแล้ว
4. ✅ SQLite database มีข้อมูล (`C:\PIMX\server\database\pim_learning.db`)

---

## 🔍 ตรวจสอบสถานะก่อน Import

```bash
npm run check-status
```

สคริปต์จะแสดง:
- ✅ Environment Variables
- ✅ Prisma Migrations
- ✅ SQLite Database (จำนวนนักเรียน)
- ✅ Supabase Connection (จำนวนนักเรียนใน Supabase)

---

## 📊 สิ่งที่สคริปต์จะทำ:

1. ✅ อ่านข้อมูลนักเรียนทั้งหมดจาก SQLite database (PIMX project)
2. ✅ ตรวจสอบว่านักเรียนมีอยู่ใน Supabase แล้วหรือไม่ (ข้ามถ้ามีอยู่แล้ว)
3. ✅ Import ข้อมูลเข้า Supabase database (batch 50 คน)
4. ✅ สร้าง password hash สำหรับนักเรียนที่ยังไม่มี (default: `student123`)
5. ✅ แสดงสรุปผลการ import

---

## 📝 ข้อมูลที่จะ Import:

- `student_id` - รหัสนักศึกษา
- `name` - ชื่อ-นามสกุล
- `email` - อีเมล (หรือสร้างจาก `student_id@pim.ac.th`)
- `password_hash` - รหัสผ่านที่ hash แล้ว
- `phone` - เบอร์โทรศัพท์
- `role` - บทบาท (student)

---

## ⚙️ การตั้งค่า

### 1. สร้างไฟล์ `.env.local`

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
DATABASE_URL=postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres
```

### 2. รัน Prisma Migrations

```bash
npx prisma migrate dev
```

---

## 📌 หมายเหตุ:

- **รหัสผ่านเริ่มต้น:** `student123` (สำหรับนักเรียนที่ยังไม่มี password_hash)
- **Duplicate Prevention:** นักเรียนที่มีอยู่แล้วใน Supabase จะถูกข้าม
- **Batch Processing:** ข้อมูลจะถูก import เป็น batch (50 คนต่อ batch) เพื่อประสิทธิภาพ
- **Progress Tracking:** สคริปต์จะแสดง progress และสรุปผลการ import

---

## 🔧 การแก้ไขปัญหา

### ❌ Error: "Supabase credentials ยังเป็น placeholder"

**วิธีแก้:** ดู [SETUP.md](./SETUP.md) - การตั้งค่า Environment Variables

### ❌ Error: "ตาราง 'users' ไม่มีใน Supabase database"

**วิธีแก้:**
```bash
npx prisma migrate dev
```

### ❌ Error: "ไม่พบ SQLite database"

**วิธีแก้:** ตรวจสอบว่าไฟล์ `C:\PIMX\server\database\pim_learning.db` มีอยู่

---

## 📚 เอกสารเพิ่มเติม

- [SETUP.md](./SETUP.md) - คู่มือการ Setup ระบบแบบครบถ้วน
- [README.md](./README.md) - เอกสารหลักของโปรเจกต์

