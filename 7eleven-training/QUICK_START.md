# 🚀 Quick Start Guide

## ขั้นตอนการ Setup ระบบ 7-Eleven Training

### 1️⃣ ติดตั้ง Dependencies

```bash
npm install
```

### 2️⃣ ตั้งค่า Environment Variables

**วิธีที่ 1: ใช้สคริปต์ช่วย (แนะนำ)**
```bash
npm run init-env
```
สคริปต์จะถามข้อมูล Supabase และสร้างไฟล์ `.env.local` ให้อัตโนมัติ

**วิธีที่ 2: สร้างเอง**
สร้างไฟล์ `.env.local`:
```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
DATABASE_URL=postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres
```

### 3️⃣ ตรวจสอบความพร้อม

```bash
npm run verify
```

### 4️⃣ Setup ทั้งหมด (อัตโนมัติ)

```bash
npm run setup
```

สคริปต์จะทำทุกอย่างให้อัตโนมัติ:
- ✅ ตรวจสอบ Supabase connection
- ✅ รัน Prisma migrations
- ✅ Import ข้อมูลนักเรียน (3,230 คน)

### 5️⃣ เริ่มใช้งาน

```bash
npm run dev
```

เปิดเบราว์เซอร์: [http://localhost:3000](http://localhost:3000)

---

## 📋 คำสั่งที่ใช้บ่อย

| คำสั่ง | คำอธิบาย |
|--------|----------|
| `npm run init-env` | ตั้งค่า environment variables แบบ interactive |
| `npm run verify` | ตรวจสอบความพร้อมของระบบ |
| `npm run check-status` | ตรวจสอบสถานะระบบ (ละเอียด) |
| `npm run setup` | Setup ทั้งหมด (migrations + import) |
| `npm run import-students` | Import ข้อมูลนักเรียน |
| `npm run dev` | เริ่ม development server |

---

## 🔍 ตรวจสอบสถานะ

```bash
npm run check-status
```

จะแสดง:
- ✅ Environment Variables
- ✅ Prisma Migrations
- ✅ SQLite Database (จำนวนนักเรียน)
- ✅ Supabase Connection

---

## ⚠️ การแก้ไขปัญหา

### ❌ Supabase credentials ยังเป็น placeholder

**แก้ไข:**
```bash
npm run init-env
```

### ❌ ตาราง 'users' ไม่มีใน Supabase

**แก้ไข:**
```bash
npx prisma migrate dev
```

### ❌ ไม่สามารถเชื่อมต่อ Supabase

**ตรวจสอบ:**
1. Internet connection
2. Supabase URL และ Key ใน `.env.local`
3. Supabase project ยัง active อยู่

---

## 📚 เอกสารเพิ่มเติม

- **[SETUP.md](./SETUP.md)** - คู่มือการ Setup แบบละเอียด
- **[README-IMPORT.md](./README-IMPORT.md)** - คำแนะนำการ Import ข้อมูล
- **[README.md](./README.md)** - เอกสารหลักของโปรเจกต์

---

## 🎉 เสร็จสมบูรณ์!

เมื่อ setup เสร็จแล้ว:
- ✅ สามารถ login ด้วยรหัสนักศึกษา
- ✅ รหัสผ่านเริ่มต้น: `student123`
- ✅ ข้อมูลนักเรียน 3,230 คนพร้อมใช้งาน

---

## 🚀 Setup แบบอัตโนมัติ (แนะนำ)

หากต้องการ setup ทั้งหมดในครั้งเดียว:

```bash
npm run setup-complete
```

สคริปต์จะช่วย:
1. ✅ ตั้งค่า Supabase credentials
2. ✅ รัน Prisma migrations
3. ✅ สร้าง Admin user
4. ✅ แสดงรหัสเข้าสู่ระบบ

**รหัส Admin ที่สร้าง:**
- รหัสนักศึกษา: `ADMIN001`
- รหัสผ่าน: `admin1234`

