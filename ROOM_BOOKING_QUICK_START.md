# 🚀 ระบบจองห้องอบรม - Quick Start Guide

## เริ่มต้นใช้งานภายใน 5 นาที!

### ✅ ขั้นตอนที่ 1: ติดตั้งและเริ่มระบบ

```bash
# 1. ติดตั้ง dependencies (ทำครั้งเดียว)
cd server
npm install

# 2. เริ่มระบบ
npm start
```

ระบบจะสร้างอัตโนมัติ:
- ✅ Database (SQLite)
- ✅ ห้องอบรม 10 ห้อง
- ✅ บัญชี Admin (username: ADMIN001, password: admin123)
- ✅ รอบสอบปฏิบัติ (12 สัปดาห์)

### ✅ ขั้นตอนที่ 2: ตรวจสอบระบบ

```bash
# ตรวจสอบว่าระบบพร้อมใช้งาน
npm run check:system
```

คุณจะเห็น:
```
✅ Users: 1 total (1 admins, 0 students)
✅ Modules: 4
✅ Rooms: 10 total (10 available)
✅ System is ready for production!
```

### ✅ ขั้นตอนที่ 3: ทดสอบระบบ

#### 🔐 Login เป็น Admin
```http
POST http://localhost:5000/api/auth/login
Content-Type: application/json

{
  "student_id": "ADMIN001",
  "password": "admin123"
}
```

คุณจะได้ `token` กลับมา

#### 🏢 ดูรายการห้อง
```http
GET http://localhost:5000/api/admin/rooms
Authorization: Bearer {your_token}
```

#### 📊 ดูแดชบอร์ด
```http
GET http://localhost:5000/api/admin/dashboard/room-bookings
Authorization: Bearer {your_token}
```

---

## 🎯 สิ่งที่แอดมินต้องรู้

### 1️⃣ เมื่อนักศึกษาจองห้อง คุณจะรู้ได้ 3 ทาง:

#### ทาง 1: กระดิ่งแจ้งเตือน 🔔
```http
GET /api/notifications/unread-count
Authorization: Bearer {token}
```
ผลลัพธ์: `{ "count": 5 }` ← มีการแจ้งเตือน 5 รายการ

#### ทาง 2: ดูการจองที่รอการอนุมัติ ⭐
```http
GET /api/admin/room-bookings/pending
Authorization: Bearer {token}
```

#### ทาง 3: แดชบอร์ดภาพรวม 📊
```http
GET /api/admin/dashboard/room-bookings
Authorization: Bearer {token}
```

### 2️⃣ อนุมัติการจอง

```http
PUT /api/admin/room-bookings/{bookingId}/status
Authorization: Bearer {token}
Content-Type: application/json

{
  "status": "approved"
}
```

สถานะที่ใช้ได้:
- `approved` ✅ อนุมัติ
- `cancelled` ❌ ปฏิเสธ
- `completed` ✔️ เสร็จสิ้น

---

## 📱 ตัวอย่าง Use Case จริง

### Use Case 1: นักศึกษาจองห้อง

**นักศึกษา:**
```http
POST /api/rooms/book
Authorization: Bearer {student_token}

{
  "room_id": "xxx",
  "booking_date": "2026-01-25",
  "start_time": "09:00",
  "end_time": "12:00"
}
```

**ระบบทำอัตโนมัติ:**
1. ✅ บันทึกการจอง (สถานะ: pending)
2. ✅ ส่งการแจ้งเตือนไปยังแอดมินทุกคน
3. ✅ ตรวจสอบไม่ให้เวลาซ้อนทับ

**แอดมินเห็น:**
- กระดิ่ง 🔔 สีแดง (1 new notification)
- ข้อความ: "สมชาย ใจดี (66010001) จองห้อง ห้องปฏิบัติการแคชเชียร์ วันที่ 25/01/2026 เวลา 09:00-12:00"

### Use Case 2: แอดมินดูการจองวันนี้

```http
GET /api/admin/room-bookings/today
Authorization: Bearer {admin_token}
```

**ผลลัพธ์:**
```json
{
  "date": "2026-01-19",
  "count": 5,
  "bookings": [
    {
      "room_name": "ห้องปฏิบัติการแคชเชียร์",
      "user_name": "สมชาย ใจดี",
      "student_id": "66010001",
      "start_time": "09:00",
      "end_time": "12:00",
      "status": "approved"
    }
  ]
}
```

---

## 🛠️ คำสั่งที่มีประโยชน์

```bash
# เริ่มระบบ
npm start

# เริ่มระบบแบบ dev (auto-reload)
npm run dev

# สร้างห้องใหม่/รีเซ็ตห้อง
npm run setup:rooms

# ตรวจสอบสถานะระบบ
npm run check:system

# สร้างข้อมูลตัวอย่าง (modules, quizzes, ฯลฯ)
npm run seed

# สร้างบัญชีแอดมิน/อาจารย์
npm run create:admin
```

---

## 📊 API Endpoints สำคัญสุด (Top 5)

### 1. แดชบอร์ดภาพรวม ⭐⭐⭐
```
GET /api/admin/dashboard/room-bookings
```
ดูสถิติทั้งหมด: รอการอนุมัติ, วันนี้, สัปดาห์นี้, เดือนนี้

### 2. การจองที่รอการอนุมัติ ⭐⭐⭐
```
GET /api/admin/room-bookings/pending
```
ดูรายการที่ต้องการความสนใจเร่งด่วน

### 3. อนุมัติ/เปลี่ยนสถานะ ⭐⭐⭐
```
PUT /api/admin/room-bookings/{id}/status
```
อนุมัติหรือปฏิเสธการจอง

### 4. การแจ้งเตือนการจองห้อง ⭐⭐
```
GET /api/notifications/room-bookings/latest
```
ดูการแจ้งเตือนการจองห้อง 20 รายการล่าสุด

### 5. การจองวันนี้ ⭐⭐
```
GET /api/admin/room-bookings/today
```
ดูกำหนดการวันนี้

---

## 🎨 ตัวอย่าง Frontend Integration

### React/Next.js Example

```jsx
// 1. ดูการจองที่รอการอนุมัติ
const [pendingBookings, setPendingBookings] = useState([]);

useEffect(() => {
  fetch('/api/admin/room-bookings/pending', {
    headers: {
      'Authorization': `Bearer ${token}`
    }
  })
  .then(res => res.json())
  .then(data => setPendingBookings(data.bookings));
}, []);

// 2. อนุมัติการจอง
const approveBooking = async (bookingId) => {
  await fetch(`/api/admin/room-bookings/${bookingId}/status`, {
    method: 'PUT',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({ status: 'approved' })
  });
  
  // รีเฟรชข้อมูล
  fetchPendingBookings();
};

// 3. แสดงจำนวนการแจ้งเตือน
const [notificationCount, setNotificationCount] = useState(0);

useEffect(() => {
  const interval = setInterval(() => {
    fetch('/api/notifications/unread-count', {
      headers: { 'Authorization': `Bearer ${token}` }
    })
    .then(res => res.json())
    .then(data => setNotificationCount(data.count));
  }, 5000); // ทุก 5 วินาที
  
  return () => clearInterval(interval);
}, []);
```

---

## ⚡ Performance Tips

### 1. ใช้ Pagination สำหรับรายการยาว
```http
GET /api/admin/room-bookings/recent?limit=20
```

### 2. ใช้ Filters ลดข้อมูล
```http
GET /api/admin/room-bookings?status=pending&date=2026-01-20
```

### 3. Cache Dashboard Data
แคชข้อมูล dashboard 30-60 วินาที

---

## 🐛 Troubleshooting

### ปัญหา: ไม่มีห้องในระบบ
```bash
npm run setup:rooms
```

### ปัญหา: Login ไม่ได้
ลอง username: `ADMIN001` / password: `admin123`

### ปัญหา: Port 5000 ถูกใช้แล้ว
แก้ไฟล์ `.env`:
```
PORT=5001
```

---

## 📞 Need Help?

1. ดูคู่มือฉบับเต็ม: `ROOM_BOOKING_SYSTEM.md`
2. ตรวจสอบ logs: `npm start` (ดู terminal)
3. รัน health check: `npm run check:system`

---

## ✅ Checklist ก่อนใช้งานจริง

- [ ] รัน `npm install` สำเร็จ
- [ ] รัน `npm start` ระบบทำงานปกติ
- [ ] รัน `npm run check:system` แสดง "System is ready"
- [ ] Login เป็น Admin ได้
- [ ] ดูรายการห้องได้ (10 ห้อง)
- [ ] ทดสอบ API endpoints สำคัญ
- [ ] ทดสอบระบบแจ้งเตือน

---

**🎉 พร้อมใช้งานแล้ว! Good luck!**

---

**เวอร์ชัน:** 1.0.0  
**อัพเดท:** 19 มกราคม 2026  
**ระบบ:** PIM Learning Platform - 7-Eleven Training
