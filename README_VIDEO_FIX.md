# 🎥 แก้ไขปัญหา Video LFS Pointer - คู่มือฉบับสมบูรณ์

## ⚡ วิธีแก้ไขแบบเร็ว (5 นาที)

### วิธีที่ 1: ใช้ Railway Dashboard (แนะนำ - ง่ายที่สุด)

1. **ไปที่ Railway Dashboard**
   - https://railway.app
   - เลือก Project ของคุณ
   - ไปที่แท็บ **"Volumes"**

2. **สร้าง Volume**
   - คลิก **"New Volume"**
   - ตั้งค่า:
     ```
     Name: video-storage
     Size: 10 GB
     Mount Path: /app/server/uploads/videos
     ```
   - คลิก **"Create"**

3. **อัปโหลดไฟล์**
   - คลิกที่ Volume ที่สร้างไว้
   - คลิก **"Upload Files"** หรือ **"Browse"**
   - เลือกไฟล์ทั้งหมดจาก `server/uploads/videos/` ในเครื่อง
   - รอให้อัปโหลดเสร็จ

4. **Restart Service**
   - ไปที่ **Deployments**
   - คลิก **"Redeploy"** หรือ restart service

5. **ทดสอบ**
   - ไปที่แอปพลิเคชัน
   - ลองเปิดวิดีโอ
   - ควรจะเล่นได้แล้ว! ✅

---

## 🔧 วิธีที่ 2: ใช้สคริปต์อัตโนมัติ

### รันสคริปต์:

```powershell
.\AUTO_FIX_VIDEO.ps1
```

สคริปต์จะ:
- ✅ ตรวจสอบ Railway CLI
- ✅ ตรวจสอบ login status
- ✅ ตรวจสอบ project link
- ✅ ตรวจสอบไฟล์วิดีโอ
- ✅ แสดงคำแนะนำขั้นตอนถัดไป

---

## 🔍 ตรวจสอบปัญหา

### ตรวจสอบว่าไฟล์เป็น LFS pointer หรือไม่:

```powershell
node server/scripts/check-video-files.js
```

### ตรวจสอบบน Railway:

```bash
railway shell
ls -lh /app/server/uploads/videos/
file /app/server/uploads/videos/video-module_1-1.mp4
```

**ควรเห็น:**
- ไฟล์มีขนาดใหญ่ (ไม่ใช่ 100-200 bytes)
- `file` command บอกว่าเป็น video file

---

## 🚨 แก้ไขปัญหา

### ปัญหา: ไฟล์ยังเป็น LFS pointer

**แก้ไข:**
1. ตรวจสอบว่า Volume mount path ถูกต้อง: `/app/server/uploads/videos`
2. ตรวจสอบว่าไฟล์อัปโหลดเสร็จแล้ว
3. Restart service

### ปัญหา: วิดีโอไม่เล่น

**ตรวจสอบ:**
1. เปิด Browser DevTools (F12)
2. Console tab → หา error messages
3. Network tab → ตรวจสอบ request `/api/video-stream/...`
4. Status code ควรเป็น 200 หรือ 206

---

## 📋 Checklist

- [ ] สร้าง Railway Volume
- [ ] Mount path: `/app/server/uploads/videos`
- [ ] อัปโหลดไฟล์วิดีโอทั้งหมด (11 ไฟล์)
- [ ] Restart service
- [ ] ตรวจสอบไฟล์บน server
- [ ] ทดสอบวิดีโอในแอปพลิเคชัน

---

## 💡 วิธีอื่น

### ใช้ External Storage (Cloudinary, S3, etc.)

ถ้า Railway Volume ไม่ทำงาน สามารถใช้:
- **Cloudinary** (ฟรี 25GB) - https://cloudinary.com
- **AWS S3**
- **Cloudflare R2** (ฟรี 10GB)
- **Google Cloud Storage**

อัปโหลดไฟล์แล้วอัปเดต URLs ใน database

---

## 📞 ต้องการความช่วยเหลือ?

1. ตรวจสอบ Railway build logs
2. ตรวจสอบ `/api/health` endpoint
3. ดู console logs ใน browser (F12)
4. ตรวจสอบ Network tab ใน browser DevTools




