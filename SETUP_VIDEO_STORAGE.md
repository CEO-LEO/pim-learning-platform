# 🎥 ตั้งค่า Video Storage บน Railway

## 📋 ขั้นตอนการตั้งค่า

### ขั้นตอนที่ 1: สร้าง Railway Volume

1. ไปที่ Railway Dashboard: https://railway.app
2. เลือก Project ของคุณ
3. ไปที่แท็บ **"Volumes"**
4. คลิก **"New Volume"**
5. ตั้งค่า:
   - **Name**: `video-storage`
   - **Size**: `10 GB` (หรือตามขนาดไฟล์วิดีโอ)
   - **Mount Path**: `/app/server/uploads/videos`
6. คลิก **"Create"**

### ขั้นตอนที่ 2: อัปโหลดไฟล์วิดีโอ

#### วิธี A: ใช้ Railway Dashboard (ง่ายที่สุด) ⭐

1. ไปที่ Volume ที่สร้างไว้
2. คลิก **"Upload Files"** หรือ **"Browse"**
3. เลือกไฟล์ทั้งหมดจาก `server/uploads/videos/` ในเครื่องของคุณ
4. รอให้อัปโหลดเสร็จ

#### วิธี B: ใช้ Railway CLI

```bash
# 1. ติดตั้ง Railway CLI (ถ้ายังไม่มี)
npm install -g @railway/cli

# 2. Login
railway login

# 3. Link project
railway link

# 4. Mount volume locally
railway volume mount

# 5. Copy ไฟล์ (ใช้ PowerShell script)
.\server\scripts\upload-videos-to-railway.ps1
```

### ขั้นตอนที่ 3: ตรวจสอบ

```bash
# SSH เข้า Railway
railway shell

# ตรวจสอบไฟล์
ls -lh /app/server/uploads/videos/
file /app/server/uploads/videos/video-module_1-1.mp4
```

### ขั้นตอนที่ 4: ทดสอบ

1. ไปที่แอปพลิเคชัน
2. ลองเปิดวิดีโอ
3. ตรวจสอบ console logs (F12)

---

## 🔍 ตรวจสอบปัญหา

### ตรวจสอบว่าไฟล์มีอยู่จริงหรือไม่:

```bash
# Local
node server/scripts/check-video-files.js

# On Railway
railway shell
cd /app/server/uploads/videos
ls -lh
file video-module_1-1.mp4
```

### ตรวจสอบผ่าน API:

```bash
curl https://your-app.railway.app/api/health
```

ดูที่ `videoFiles.hasFiles` และ `videoFiles.lfsPointers`

---

## 🚨 ถ้ายังมีปัญหา

### ปัญหา: ไฟล์ยังเป็น LFS pointer

**แก้ไข:**
1. ตรวจสอบว่า Volume mount path ถูกต้อง: `/app/server/uploads/videos`
2. ตรวจสอบว่าไฟล์อัปโหลดเสร็จแล้ว
3. Restart service บน Railway

### ปัญหา: วิดีโอไม่เล่น

**ตรวจสอบ:**
1. เปิด Browser DevTools (F12)
2. ดู Console tab - หา error messages
3. ดู Network tab - ตรวจสอบ request ไปที่ `/api/video-stream/...`
4. ตรวจสอบ status code (ควรเป็น 200 หรือ 206)

---

## ✅ Checklist

- [ ] สร้าง Railway Volume
- [ ] Mount path ถูกต้อง: `/app/server/uploads/videos`
- [ ] อัปโหลดไฟล์วิดีโอทั้งหมด
- [ ] ตรวจสอบว่าไฟล์มีอยู่บน server
- [ ] ทดสอบวิดีโอในแอปพลิเคชัน
- [ ] ตรวจสอบ `/api/health` endpoint

---

## 📞 ต้องการความช่วยเหลือ?

1. ตรวจสอบ Railway build logs
2. ตรวจสอบ `/api/health` endpoint
3. ดู console logs ใน browser (F12)
4. ตรวจสอบ Network tab ใน browser DevTools

