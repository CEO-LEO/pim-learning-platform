[phases.setup]
nixPkgs = ["git", "git-lfs", "nodejs-18_x", "npm"]

[phases.install]
cmds = [
  "git lfs install",
  "git lfs pull",
  "ls -la server/uploads/videos/ || echo 'Video directory check'",
  "cd server && npm install",
  "cd ../client && npm install"
]

[phases.build]
cmds = ["cd client && npm run build"]

[start]
cmd = "cd server && node index.js"

