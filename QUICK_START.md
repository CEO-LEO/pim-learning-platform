# 🚀 คู่มือเริ่มต้นใช้งานด่วน

## การแก้ปัญหา ERR_CONNECTION_REFUSED

หากคุณเห็นข้อผิดพลาด "localhost ปฏิเสธที่จะเชื่อมต่อ" นั่นหมายความว่าเซิร์ฟเวอร์ยังไม่ได้รัน

## วิธีรันแอปพลิเคชัน

### วิธีที่ 1: ใช้สคริปต์อัตโนมัติ (แนะนำ)

**Windows:**
```bash
start-dev.bat
```

**PowerShell:**
```powershell
.\start-dev.ps1
```

### วิธีที่ 2: รันแยกกัน

#### 1. รัน Backend Server (Port 5000)
```bash
cd server
npm install  # ถ้ายังไม่ได้ติดตั้ง
npm start
```

#### 2. รัน React Client (Port 3000)
เปิดหน้าต่าง Terminal ใหม่:
```bash
cd client
npm install  # ถ้ายังไม่ได้ติดตั้ง
npm start
```

## ตรวจสอบการตั้งค่า

### ไฟล์ .env

**server/.env** (ควรมีอยู่แล้ว):
```
PORT=5000
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production_123456789
DATABASE_URL=sqlite:./database/pim_learning.db
API_URL=http://localhost:5000/api
```

**client/.env** (ควรมีอยู่แล้ว):
```
REACT_APP_API_URL=http://localhost:5000/api
```

หากไฟล์ .env ไม่มีอยู่ ให้รัน:
- `server/env-setup.bat` สำหรับ server
- `client/env-setup.bat` สำหรับ client

## ตรวจสอบว่าเซิร์ฟเวอร์รันอยู่

1. **Backend Server**: เปิดเบราว์เซอร์ไปที่ http://localhost:5000
   - ควรเห็นหน้า "PIM Learning Platform API"

2. **React Client**: เปิดเบราว์เซอร์ไปที่ http://localhost:3000
   - ควรเห็นหน้าแอปพลิเคชัน

## ปัญหาที่พบบ่อย

### Port ถูกใช้งานแล้ว
หาก port 5000 หรือ 3000 ถูกใช้งานแล้ว:
- ปิดโปรแกรมที่ใช้ port นั้น
- หรือเปลี่ยน port ในไฟล์ .env

### ไม่สามารถติดตั้ง dependencies
```bash
# ลบ node_modules และติดตั้งใหม่
cd server
rm -rf node_modules
npm install

cd ../client
rm -rf node_modules
npm install
```

### Database ไม่พบ
ตรวจสอบว่าไฟล์ `server/database/pim_learning.db` มีอยู่
หากไม่มี ให้รัน:
```bash
cd server/database
node init.js
```

## 📞 ติดต่อ

หากยังมีปัญหา ให้ตรวจสอบ:
- Node.js ติดตั้งแล้ว (เวอร์ชัน 14 ขึ้นไป)
- npm ติดตั้งแล้ว
- ไม่มีไฟร์วอลล์บล็อก port 5000 และ 3000

