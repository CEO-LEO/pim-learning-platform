# 🏢 ระบบจองห้องอบรม - คู่มือการใช้งาน

## 📋 สารบัญ
1. [ภาพรวมระบบ](#ภาพรวมระบบ)
2. [API Endpoints](#api-endpoints)
3. [การตั้งค่าระบบ](#การตั้งค่าระบบ)
4. [สำหรับแอดมิน](#สำหรับแอดมิน)
5. [สำหรับนักศึกษา](#สำหรับนักศึกษา)
6. [ระบบแจ้งเตือน](#ระบบแจ้งเตือน)

---

## 🎯 ภาพรวมระบบ

ระบบจองห้องอบรมสำหรับ 7-Eleven Demonstration Store Training ที่มีฟีเจอร์ครบครัน:

### ✨ ฟีเจอร์หลัก
- ✅ จองห้องอบรมออนไลน์
- ✅ ตรวจสอบความว่างของห้อง
- ✅ อนุมัติ/ปฏิเสธการจอง
- ✅ แจ้งเตือนอัตโนมัติ
- ✅ แดชบอร์ดสำหรับแอดมิน
- ✅ ประวัติการจอง

### 🏠 ห้องอบรมที่มีในระบบ
1. **ห้องปฏิบัติการแคชเชียร์** (20 ที่นั่ง) - POS training
2. **ห้องเตรียมสินค้าอุ่นร้อน** (15 ที่นั่ง) - Food prep
3. **ห้องจัดเรียงสินค้า** (25 ที่นั่ง) - Merchandising
4. **ห้องคลังสินค้า** (15 ที่นั่ง) - Storage management
5. **ห้องบรรยาย A** (50 ที่นั่ง) - Large lectures
6. **ห้องบรรยาย B** (30 ที่นั่ง) - Medium lectures
7. **ห้องประชุมกลุ่มย่อย** (12 ที่นั่ง) - Group discussions
8. **Demo Store** (10 ที่นั่ง) - Practical training
9. **ห้องฝึกอบรมคุณภาพอาหาร** (18 ที่นั่ง) - Food safety
10. **ห้องคอมพิวเตอร์** (20 ที่นั่ง) - Computer lab

---

## 🔌 API Endpoints

### 👥 สำหรับนักศึกษา (Student APIs)

#### 1. ดูรายการห้องทั้งหมด
```http
GET /api/rooms/list
Authorization: Bearer {token}
```

**Response:**
```json
[
  {
    "room_id": "xxx",
    "name": "ห้องปฏิบัติการแคชเชียร์",
    "description": "สำหรับฝึกทักษะการใช้เครื่อง POS",
    "capacity": 20,
    "status": "available"
  }
]
```

#### 2. ดูการจองของห้องในวันที่ระบุ
```http
GET /api/rooms/bookings/{roomId}/{date}
Authorization: Bearer {token}
```
- `date` format: `YYYY-MM-DD` (เช่น `2026-01-20`)

#### 3. จองห้อง
```http
POST /api/rooms/book
Authorization: Bearer {token}
Content-Type: application/json

{
  "room_id": "xxx",
  "booking_date": "2026-01-20",
  "start_time": "09:00",
  "end_time": "12:00"
}
```

**Response:**
```json
{
  "message": "จองห้องสำเร็จ",
  "booking_id": "yyy"
}
```

#### 4. ดูการจองของฉัน
```http
GET /api/rooms/my-bookings
Authorization: Bearer {token}
```

#### 5. ยกเลิกการจอง
```http
POST /api/rooms/cancel
Authorization: Bearer {token}
Content-Type: application/json

{
  "booking_id": "yyy"
}
```

---

### 👨‍💼 สำหรับแอดมิน (Admin APIs)

#### 📊 แดชบอร์ด

##### 1. ภาพรวมการจองทั้งหมด ⭐
```http
GET /api/admin/dashboard/room-bookings
Authorization: Bearer {token}
```

**Response:**
```json
{
  "summary": {
    "pendingBookings": 5,      // รอการอนุมัติ
    "todayBookings": 8,         // จองวันนี้
    "weekBookings": 32,         // จองสัปดาห์นี้
    "monthBookings": 127,       // จองเดือนนี้
    "totalRooms": 10           // จำนวนห้องทั้งหมด
  },
  "recentBookings": [...]
}
```

##### 2. การจองที่รอการอนุมัติ ⭐⭐⭐
```http
GET /api/admin/room-bookings/pending
Authorization: Bearer {token}
```

**Response:**
```json
{
  "count": 5,
  "bookings": [
    {
      "booking_id": "xxx",
      "room_name": "ห้องปฏิบัติการแคชเชียร์",
      "user_name": "สมชาย ใจดี",
      "student_id": "66010001",
      "phone": "081-234-5678",
      "booking_date": "2026-01-20",
      "start_time": "09:00",
      "end_time": "12:00",
      "status": "pending",
      "created_at": "2026-01-19T10:30:00Z"
    }
  ]
}
```

##### 3. การจองวันนี้
```http
GET /api/admin/room-bookings/today
Authorization: Bearer {token}
```

##### 4. การจองล่าสุด
```http
GET /api/admin/room-bookings/recent?limit=20
Authorization: Bearer {token}
```

#### 🏢 จัดการห้อง

##### 1. ดูรายการห้องทั้งหมด
```http
GET /api/admin/rooms
Authorization: Bearer {token}
```

##### 2. สร้างห้องใหม่
```http
POST /api/admin/rooms
Authorization: Bearer {token}
Content-Type: application/json

{
  "name": "ห้องใหม่",
  "description": "คำอธิบาย",
  "capacity": 20
}
```

##### 3. แก้ไขข้อมูลห้อง
```http
PUT /api/admin/rooms/{roomId}
Authorization: Bearer {token}
Content-Type: application/json

{
  "name": "ห้องใหม่",
  "description": "คำอธิบาย",
  "capacity": 25,
  "status": "available"
}
```

##### 4. ลบห้อง
```http
DELETE /api/admin/rooms/{roomId}
Authorization: Bearer {token}
```

#### 📋 จัดการการจอง

##### 1. ดูการจองทั้งหมด (พร้อม filters)
```http
GET /api/admin/room-bookings?status=pending&date=2026-01-20
Authorization: Bearer {token}
```

**Query Parameters:**
- `room_id` - กรองตามห้อง
- `status` - กรองตามสถานะ (pending, approved, cancelled, completed)
- `date` - กรองตามวันที่ (YYYY-MM-DD)
- `user_id` - กรองตามผู้จอง

##### 2. อนุมัติ/เปลี่ยนสถานะการจอง ⭐
```http
PUT /api/admin/room-bookings/{bookingId}/status
Authorization: Bearer {token}
Content-Type: application/json

{
  "status": "approved"
}
```

**สถานะที่ใช้ได้:**
- `pending` - รอการอนุมัติ
- `approved` - อนุมัติแล้ว ✅
- `cancelled` - ยกเลิก ❌
- `completed` - เสร็จสิ้น ✅

##### 3. สร้างการจองโดยแอดมิน
```http
POST /api/admin/room-bookings
Authorization: Bearer {token}
Content-Type: application/json

{
  "room_id": "xxx",
  "user_id": "yyy",
  "booking_date": "2026-01-20",
  "start_time": "09:00",
  "end_time": "12:00",
  "status": "approved"
}
```

##### 4. ลบการจอง
```http
DELETE /api/admin/room-bookings/{bookingId}
Authorization: Bearer {token}
```

##### 5. ดูการจองของห้องเฉพาะ
```http
GET /api/admin/room-bookings/room/{roomId}?date=2026-01-20
Authorization: Bearer {token}
```

##### 6. สถิติการจอง
```http
GET /api/admin/room-bookings/statistics
Authorization: Bearer {token}
```

---

## 🔔 ระบบแจ้งเตือน

### API สำหรับ Notifications

#### 1. ดูการแจ้งเตือนทั้งหมด
```http
GET /api/notifications
Authorization: Bearer {token}
```

#### 2. ดูจำนวนการแจ้งเตือนที่ยังไม่ได้อ่าน
```http
GET /api/notifications/unread-count
Authorization: Bearer {token}
```

**Response:**
```json
{
  "count": 5
}
```

#### 3. ดูการแจ้งเตือนการจองห้อง (Admin only)
```http
GET /api/notifications/room-bookings/latest
Authorization: Bearer {token}
```

#### 4. ทำเครื่องหมายว่าอ่านแล้ว
```http
PUT /api/notifications/{notificationId}/read
Authorization: Bearer {token}
```

#### 5. ทำเครื่องหมายทั้งหมดว่าอ่านแล้ว
```http
PUT /api/notifications/read-all
Authorization: Bearer {token}
```

### 🔔 การแจ้งเตือนอัตโนมัติ

เมื่อนักศึกษาจองห้อง ระบบจะ**ส่งการแจ้งเตือนอัตโนมัติ**ไปยัง:
- ✅ แอดมินทุกคน
- ✅ อาจารย์ผู้สอนทุกคน (role: instructor)

**รูปแบบการแจ้งเตือน:**
```
🔔 มีการจองห้องใหม่
สมชาย ใจดี (66010001) จองห้อง ห้องปฏิบัติการแคชเชียร์ 
วันที่ 2026-01-20 เวลา 09:00-12:00 น.
```

---

## ⚙️ การตั้งค่าระบบ

### 1. ติดตั้งและเริ่มระบบครั้งแรก

```bash
# ติดตั้ง dependencies
cd server
npm install

# เริ่มระบบ (จะสร้าง database และห้องอบรมอัตโนมัติ)
npm start
```

### 2. สร้างห้องอบรมใหม่ (ถ้าต้องการ)

```bash
# สร้าง/รีเซ็ตห้องอบรม
node server/database/setup-rooms.js
```

### 3. ตรวจสอบสถานะระบบ

```bash
# ตรวจสอบว่าระบบพร้อมใช้งานหรือไม่
node server/database/check-system.js
```

**Output ตัวอย่าง:**
```
🔍 Checking system status...

✅ Users: 15 total (2 admins, 13 students)
✅ Modules: 4
✅ Videos: 12
✅ Quizzes: 8
✅ Rooms: 10 total (10 available)
✅ Room Bookings: 45 total (5 pending, 30 approved)
✅ Practical Exams: 120 total (100 open)
✅ Notifications: 23 total (8 unread)

📊 SYSTEM SUMMARY
==================================================
✅ System is ready for production!
```

---

## 🎯 Use Cases

### สถานการณ์ที่ 1: นักศึกษาจองห้อง

1. นักศึกษาเข้าสู่ระบบ
2. เลือกเมนู "จองห้องฝึกปฏิบัติ"
3. เลือกห้องและเวลาที่ต้องการ
4. กดจอง → สถานะ `pending`
5. **แอดมินได้รับการแจ้งเตือนทันที** 🔔

### สถานการณ์ที่ 2: แอดมินอนุมัติการจอง

1. แอดมินเห็นกระดิ่งแจ้งเตือนสีแดง (มีการจองใหม่)
2. คลิกดู → เห็นรายละเอียดการจอง
3. ตรวจสอบว่าห้องว่าง
4. กด "อนุมัติ" → สถานะเปลี่ยนเป็น `approved`
5. นักศึกษาได้รับการแจ้งเตือนว่าการจองได้รับการอนุมัติ

### สถานการณ์ที่ 3: ดูภาพรวมการจองวันนี้

1. แอดมินเข้าสู่หน้าแดชบอร์ด
2. เห็นการ์ด "การจองวันนี้" แสดง 8 รายการ
3. คลิกดูรายละเอียด → เห็นตารางเวลาทั้งหมด
4. ตรวจสอบว่าห้องไหนใช้งานอยู่

---

## 📝 สถานะการจอง (Booking Status)

| สถานะ | ความหมาย | ใช้เมื่อ |
|-------|----------|---------|
| `pending` | รอการอนุมัติ | นักศึกษาจองใหม่ |
| `approved` | อนุมัติแล้ว | แอดมินอนุมัติ |
| `cancelled` | ยกเลิกแล้ว | นักศึกษาหรือแอดมินยกเลิก |
| `completed` | เสร็จสิ้น | หลังจากใช้ห้องเสร็จ |

---

## 🛡️ Security & Permissions

### สิทธิ์การเข้าถึง

| Endpoint | Student | Admin/Instructor |
|----------|---------|------------------|
| `/api/rooms/*` (นักศึกษา) | ✅ | ✅ |
| `/api/admin/rooms` | ❌ | ✅ |
| `/api/admin/room-bookings` | ❌ | ✅ |
| `/api/admin/dashboard` | ❌ | ✅ |

---

## 🐛 Troubleshooting

### ปัญหาที่พบบ่อย

#### 1. ไม่มีห้องในระบบ
```bash
node server/database/setup-rooms.js
```

#### 2. แอดมินไม่ได้รับการแจ้งเตือน
- ตรวจสอบว่ามี admin ในระบบ
- ตรวจสอบ console logs
- ลองจองใหม่อีกครั้ง

#### 3. ไม่สามารถจองห้องได้ (ช่วงเวลาซ้อนทับ)
- ตรวจสอบว่าช่วงเวลาว่างหรือไม่
- ลองเลือกเวลาอื่น

---

## 📞 Support

หากมีปัญหาหรือข้อสงสัย:
1. ตรวจสอบ logs: `npm start` (ดู console)
2. รัน health check: `node server/database/check-system.js`
3. ตรวจสอบ database: ใช้ SQLite Browser

---

## 🎉 สรุป

ระบบจองห้องอบรมถูกสร้างมาเพื่อ:
- ✅ **ง่ายต่อการใช้งาน** - UI/UX ที่เข้าใจง่าย
- ✅ **รวดเร็ว** - แจ้งเตือนแบบ real-time
- ✅ **ปลอดภัย** - มีระบบ authentication และ authorization
- ✅ **สมบูรณ์** - ครอบคลุมทุก use case
- ✅ **พร้อมใช้งานจริง** - Production-ready!

---

**Version:** 1.0.0  
**Last Updated:** 2026-01-19  
**System:** PIM Learning Platform - 7-Eleven Training
