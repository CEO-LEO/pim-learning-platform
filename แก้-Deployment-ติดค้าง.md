# ⚠️ แก้ Deployment ติดค้าง

## ❌ ปัญหา

Deployment ใช้เวลา **19 นาที** ซึ่งนานเกินไป (ปกติควรใช้เวลา 2-5 นาที)

**Status:** "Deployment in progress: Running pre-deploy command..."

---

## 🔍 วิธีตรวจสอบ

### Step 1: ดู Deploy Logs

1. ไปที่ **Deploy Logs** tab (ไม่ใช่ Build Logs)
2. ดู log ล่าสุด
3. ตรวจสอบว่า:
   - ✅ กำลัง build อยู่หรือไม่
   - ✅ กำลัง install dependencies อยู่หรือไม่
   - ❌ ติดค้างที่ command ไหน
   - ❌ มี error message อะไร

---

### Step 2: ตรวจสอบ Root Directory

1. ไปที่ **Settings** tab
2. หา section: **Deploy** หรือ **Build & Deploy**
3. ตรวจสอบว่า **Root Directory** = `server` หรือไม่
4. ถ้ายังไม่ได้ตั้ง → **ตั้งเป็น `server`** แล้ว **Save**

---

## 🔧 วิธีแก้

### วิธีที่ 1: Cancel และ Redeploy

1. **Cancel deployment ปัจจุบัน:**
   - คลิก **...** (สามจุด) ที่ deployment ที่กำลัง deploy
   - เลือก **Cancel** (ถ้ามี)

2. **ตั้ง Root Directory:**
   - ไปที่ **Settings** → **Deploy**
   - ตั้ง **Root Directory** = `server`
   - **Save**

3. **Redeploy:**
   - คลิก **Redeploy** ที่ deployment ล่าสุด
   - หรือ **Push code ใหม่** ไปที่ GitHub

---

### วิธีที่ 2: ตรวจสอบ Deploy Logs

1. ไปที่ **Deploy Logs** tab
2. ดู log ล่าสุด
3. หา error message หรือ command ที่ติดค้าง
4. แก้ไขตามที่พบ

---

## 🚨 ปัญหาที่เป็นไปได้

### 1. Pre-deploy Command ติดค้าง

**สาเหตุ:**
- Command ใช้เวลานานเกินไป
- Command มี infinite loop

**วิธีแก้:**
- Cancel deployment
- ตรวจสอบ `nixpacks.toml` และ `railway.json`
- ลบ pre-deploy command (ถ้าไม่จำเป็น)

---

### 2. Build Process ติดค้าง

**สาเหตุ:**
- npm install ใช้เวลานาน
- Dependencies มีปัญหา

**วิธีแก้:**
- Cancel deployment
- ตรวจสอบ `package.json` ใน `server/`
- ลองใช้ `npm ci` แทน `npm install`

---

### 3. Root Directory ไม่ถูกต้อง

**สาเหตุ:**
- Railway ไม่รู้ว่า Root Directory อยู่ที่ไหน
- กำลังหาไฟล์ผิดที่

**วิธีแก้:**
- ตั้ง **Root Directory** = `server` ใน Railway Settings
- Save และ Redeploy

---

## 📋 Checklist

- [ ] ดู Deploy Logs เพื่อหา error
- [ ] ตรวจสอบ Root Directory = `server` ใน Settings
- [ ] Cancel deployment ที่ติดค้าง (ถ้าจำเป็น)
- [ ] Redeploy ใหม่
- [ ] ตรวจสอบว่า deployment เสร็จภายใน 5 นาที

---

## 💡 Tips

- **Deployment ปกติ** ควรใช้เวลา **2-5 นาที**
- **ถ้าใช้เวลานานกว่า 10 นาที** → มีปัญหา
- **แนะนำให้ Cancel และ Redeploy** หลังจากตั้ง Root Directory แล้ว

---

## 🔄 ขั้นตอนแนะนำ

1. **Cancel deployment ปัจจุบัน** (ถ้าเป็นไปได้)
2. **ตั้ง Root Directory = `server`** ใน Railway Settings
3. **Save**
4. **Redeploy** (Railway จะ auto-redeploy หรือคลิก Redeploy)
5. **รอ 2-5 นาที**
6. **ตรวจสอบ Deploy Logs** ว่าไม่มี error

